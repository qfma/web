name: Your New Jekyll Site
markdown: redcarpet
pygments: true

# Use a separate source and build folder:
source: ./src
destination: ./build

# Configuration for the asset pipeline
assets:
  sources:
    - _assets/images
    - _assets/javascripts
    - _assets/stylesheets
    - bower_components
  compress:
    css: sass
    js: uglifier